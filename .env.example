# .env.example — Copy this to .env and fill in your credentials
# This file IS committed to GitHub as a template (no real secrets here)
# .env itself is gitignored and NEVER committed

GRAFANA_USER=your_username_here
GRAFANA_PASSWORD=your_strong_password_here
